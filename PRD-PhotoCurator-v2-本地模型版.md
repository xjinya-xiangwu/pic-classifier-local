# PRD v2 — PhotoCurator 本地模型版（完全离线方案）

| 文档信息 | 内容 |
| --- | --- |
| 产品名称 | PhotoCurator v2「本地模型版」 |
| 文档版本 | v2.0（草案，待评审） |
| 编写日期 | 2026-09-22 |
| 分支 | `feature/local-model`（基于 main @ `117e8a6`） |
| 前置文档 | [PRD v1](PRD-PhotoCurator-服装照片智能分组整理App.md)（API 方案，MVP 已上线） |
| 目标硬件 | MacBook Pro M4 Max（36 / 48 / 64 / 128GB 统一内存，内存带宽 ~546GB/s），macOS 15.1+ |

---

## 1. 摘要

v2 用**跑在用户自己 Mac 上的开源视觉模型（MLX 运行时）替代云端 API**：照片从扫描、识别到分组、重命名全程不出本机，API 费用归零，断网可用。v1 已验证的产品形态（扫描 → 结构化打标 → 三轴分组 → 人工验收 → 原地重命名/撤销）与全部数据层设计**原样保留**；v2 替换的只是「识别」这一环的推理后端，并把 API 模式降级为可选项。

## 2. 背景与动机

v1（GLM API 方案）已交付并在真实拍摄集上可用，但存在四个结构性问题：

1. **隐私**：服装照片是商业资产，压缩图仍需发送到外部服务（boyue 中转 / 智谱），部分客户场景不可接受；
2. **成本与依赖**：按张计费随量增长；中转站可用性、Key 管理都是长期运维负担；
3. **网络**：识别期间强依赖网络，拍摄外勤场景无法使用；
4. **硬件契机**：目标用户换装 M4 Max MacBook——统一内存架构 + MLX 生态（Apple 官方维护的开源推理框架）使 30B 级视觉模型在笔记本上达到可用吞吐，v1 立项时「本地模型慢且效果不稳」的前提已不成立。

## 3. v1 → v2 变更摘要

| 维度 | v1（API 版，已上线） | v2（本地模型版） |
| --- | --- | --- |
| 识别后端 | GLM 5.3 flash（boyue 中转，OpenAI 兼容 HTTP） | 本地 MLX 推理服务（OpenAI 兼容 HTTP，127.0.0.1） |
| 照片出域 | 压缩图发送外部服务 | **完全不出本机** |
| 单批成本 | 千张约 ¥5–15 | **¥0** |
| 千张耗时 | ~20 分钟（并发 6） | 预计 1–3 小时（串行，档位依赖，L1 里程碑实测） |
| 首次准备 | 粘贴 API Key | 下载模型（3–19GB，一次性，支持国内镜像与断点续传） |
| 并发 | 6 | **1**（单 GPU 串行；拼批优化列为可选项） |
| API 模式 | 唯一模式 | 保留为可切换选项（外勤无网→本地；赶时间→API） |
| 其余（扫描/分组/验收/重命名/撤销/断点续跑/缓存） | 不变 | **不变，代码直接复用** |

## 4. 目标与关键结果（KR-L）

| # | 关键结果 | 验收方式 |
| --- | --- | --- |
| KR-L1 | **完全离线**：模型就位后，断网状态下可完成「扫描→识别→分组→验收→重命名→撤销」全流程 | 关闭 Wi-Fi 全流程实测 |
| KR-L2 | 千张识别耗时：30B-A3B 档 ≤ 3 小时；8B 档 ≤ 2 小时（M4 Max，含硬盘读取） | 性能基准测试（L1 里程碑校准） |
| KR-L3 | 分组质量对齐 API 版：金标准集 200 张上组纯度/召回 F1 ≥ 0.85（API 版基线 0.9，允许 5% 退化） | 与 v1 相同评测脚本 |
| KR-L4 | 模型一键下载/切换：设置页选择模型 → 显示下载进度与磁盘需求 → 断点续传 → 完成即用 | 中断下载后重启续传实测 |
| KR-L5 | 内存安全：推理峰值内存 < 机器物理内存 × 75%；不满足时拒绝加载并建议更小档位 | 内存监视脚本 |
| KR-L6 | API 模式保留：设置页可随时在「本地 / API」间切换，两模式标签缓存互不干扰 | 双模式交叉使用实测 |

## 5. 模型选型（Hugging Face 调研结论，2026-09-22）

筛选标准：多模态 VLM、有 **mlx-community 官方 MLX 移植**、中文结构与 JSON 输出能力强、License 可商用、体积适配 M4 Max。

| 档位 | 模型（mlx-community 4bit） | 权重体积 | 推理内存（含 KV/视觉塔，估） | 定位 |
| --- | --- | --- | --- | --- |
| **主力推荐** | [Qwen3-VL-30B-A3B-Instruct-4bit](https://hf.co/mlx-community/Qwen3-VL-30B-A3B-Instruct-4bit) | 18.3GB | ~22–24GB | MoE 架构：总参 30B、**每 token 仅激活 3B** → 生成速度接近 4B 小模型、理解质量接近 30B 大模型。48GB+ 机型默认 |
| 均衡 | [Qwen3-VL-8B-Instruct-4bit](https://hf.co/mlx-community/Qwen3-VL-8B-Instruct-4bit) | 5.8GB | ~8–9GB | 36GB 机型稳妥默认；质量/速度均衡 |
| 快速 | [Qwen3-VL-4B-Instruct-4bit](https://hf.co/mlx-community/Qwen3-VL-4B-Instruct-4bit) | 3.1GB | ~5–6GB | 预览/低配档；HF 下载量 129K，社区验证充分 |

**全部为 Apache-2.0 License，商用无限制。** Qwen 系是中文结构化输出（本应用枚举 JSON 契约）的最强开源自选。

备选与观察：
- [gemma-3-12b-it-4bit](https://hf.co/mlx-community/gemma-3-12b-it-4bit)（HF 下载 419K，生态最热）：中文 JSON 遵从度弱于 Qwen，且 Gemma License 有附加条款 → 不选；
- [gemma-4-31b-it-4bit](https://hf.co/mlx-community/gemma-4-31b-it-4bit)（2026-04 新发，Apache-2.0，含 apple-silicon 优化量化版）：质量有竞争力，中文待验证 → 列为 L1 里程碑实测观察对象；
- InternVL3 系列（mlx 移植下载量低、生态薄）、MiniCPM-V（MLX 维护停滞）→ 不选。

## 6. 技术方案

### 6.1 架构：推理后端插件化，HTTP 契约不变

v1 的核心架构决策——「识别 = OpenAI 兼容 HTTP 调用」——在 v2 直接兑现价值：**本地推理服务同样暴露 OpenAI 兼容的 `/v1/chat/completions`**，app.py 现有的 `vlm_tag()` 客户端、退避重试、用量统计、断点续跑**零改动复用**，只切换 `base_url`。

```mermaid
flowchart LR
    subgraph 不变层[v1 复用，零改动]
        UI[浏览器验收界面]
        SCAN[扫描/缩略图]
        GRP[分组引擎]
        RENAME[重命名/撤销]
        DB[(SQLite)]
        CLIENT[vlm_tag 客户端\nOpenAI 兼容 HTTP + 退避重试]
    end
    subgraph v2 新增[本地推理引擎 (新模块)]
        MGR[模型管理器\n下载/校验/删除 (huggingface_hub\n支持 HF_ENDPOINT 国内镜像)]
        SRV[mlx-lm server 子进程\nOpenAI 兼容 · 127.0.0.1:随机端口]
        PRE[内存预检\n物理内存 × 0.75 判定]
    end
    subgraph 后端选择
        LOCAL[本地模式\nSRV ← CLIENT]
        API[API 模式 (保留)\nboyue/GLM ← CLIENT]
    end
    CLIENT -->|mode=local| SRV
    CLIENT -->|mode=api| CLOUD[云端 API]
```

### 6.2 关键设计决策

| # | 决策 | 理由 |
| --- | --- | --- |
| D1 | 运行时选 **MLX**（mlx-vlm + mlx-lm server），不用 llama.cpp/Ollama | Apple 官方生态、M 系芯片第一优先优化；Qwen3-VL MoE 的 MLX 支持成熟；OpenAI 兼容 server 自带。Ollama 作为用户自选兜底（若用户已装，可直接指 base_url） |
| D2 | 本地服务为 **App 管理的子进程**（随 App 启动/退出），随机端口 + 仅监听 127.0.0.1 | 用户无感知；不与系统其他服务冲突；安全边界与本机一致 |
| D3 | 本地模式**并发 = 1** | 单 GPU 串行最快（并发请求在 MLX 内排队反而增加峰值内存）。千张 2–3 小时是「挂机成本」而非「等待成本」——进度条 + 断点续跑已覆盖体验。多图拼批（一次推理 2–4 图）列为 L3 可选优化 |
| D4 | 图片预处理不变（长边 1024px JPEG q85） | 视觉 token 数与质量平衡已在 v1 验证；本地模式无带宽顾虑但仍受 KV cache 内存约束 |
| D5 | Prompt/枚举契约不变 | 分组引擎、组名生成全部复用；新增「**JSON 修复重试**」：校验失败时把错误信息回喂模型重试 1 次（本地模型 JSON 遵从度低于旗舰 API，这是质量保底关键） |
| D6 | 模型存 `~/PhotoCurator/models/`，用 huggingface_hub 下载 | 天然断点续传；`HF_ENDPOINT=https://hf-mirror.com` 环境变量自动启用国内镜像（下载页提供开关）；下载前校验磁盘剩余空间 ≥ 体积 × 1.2 |
| D7 | 设置页新增「运行模式」三态：本地 / API /（默认）本地 | API 模式完整保留（KR-L6）；两模式标签缓存共用（按文件哈希键，与后端无关） |

### 6.3 性能预算（M4 Max 实测前的工作假设，L1 校准）

| 档位 | 单张耗时构成（估） | 千张总耗时（估） |
| --- | --- | --- |
| 30B-A3B-4bit | 预填 ~2–4s + 生成 ~250 tok ÷ ~45 tok/s ≈ 5–6s → **8–10s/张** | 2.2–2.8 小时 |
| 8B-4bit | 预填 ~1.5s + 生成 ~250 tok ÷ ~30 tok/s ≈ 8s → **10s/张** | ~2.8 小时（MoE 反超） |
| 4B-4bit | **4–6s/张** | 1.1–1.7 小时 |

> 注：MoE 的 30B-A3B 生成速度（激活 3B）**快于** dense 8B，这是选它做主力档的核心依据；预填（视觉编码）三档差异较小。上表为理论推算，L1 里程碑以真机实测校准后写死进本文档。

### 6.4 改动清单（相对 v1 代码库）

| 模块 | 改动 | 量级 |
| --- | --- | --- |
| `app.py` 新增 `local_engine.py` 或同文件新节 | 模型管理器（列模型/下载/进度/删除/磁盘与内存预检）+ 子进程管理（启停 mlx server、健康检查、崩溃重启） | 新增 ~300 行 |
| `vlm_tag()` | 增加本地模式 URL 指向 + JSON 修复重试 | ~20 行 |
| 调度器 | 本地模式强制 concurrency=1；跳过 429 退避逻辑（本地无限流） | ~10 行 |
| 设置页/状态 API | 运行模式切换、模型选择、下载进度条、内存/磁盘提示 | 前端 ~100 行 |
| 安装器 `install_mac.sh` | 依赖加 `mlx-lm mlx-vlm huggingface_hub`（约 +300MB 磁盘）；MLX 仅 macOS 可装，requirements 拆分 | ~20 行 |
| 其余（扫描/分组/验收/重命名/撤销/界面） | **不改** | 0 |

### 6.5 数据与文件布局（增量）

```
~/PhotoCurator/
├── models/                          ← 新增：模型权重
│   └── Qwen3-VL-30B-A3B-Instruct-4bit/   (~18.3GB)
└── app.log
~/.photocurator/                     ← 不变：数据库/缩略图/撤销记录/清单
```

## 7. 假设与风险

**假设（L1 验证）：**

1. Qwen3-VL-4B/8B/30B-A3B 在 1024px 输入下对本应用中文枚举 JSON 的首 pass 遵从率 ≥ 85%，修复重试后 ≥ 98%；
2. M4 Max（36GB 起）承载上表三档内存预算无压力；
3. 用户接受「首次下载 3–19GB 模型」与「千张 1–3 小时挂机」两个新代价，换取零成本与完全离线；
4. MLX 生态在项目周期内保持对 Qwen3-VL 的版本兼容（pin 版本号缓解）。

**风险与对策：**

| 风险 | 概率 | 影响 | 对策 |
| --- | --- | --- | --- |
| 本地模型 JSON 输出不稳定 | 高 | 标签缺失/非法 → 分组质量下降 | D5 修复重试 + v1 已有 `validate_tag` 归一兜底；L1 实测各档遵从率 |
| 速度不达 KR-L2 | 中 | 体验劣化 | L1 真机实测定档；拼批优化备用；API 模式随时可切 |
| 36GB 机型加载 30B-A3B 后系统内存吃紧 | 中 | 触发 swap/被系统杀进程 | 内存预检（KR-L5）按机型推荐档位；默认 48GB+ 才给 30B |
| 19GB 下载失败/中断 | 中 | 首次体验差 | hf-mirror 镜像 + huggingface_hub 断点续传 + 磁盘预检 |
| mlx-vlm 版本兼容性破坏 | 中 | 升级后推理失败 | requirements pin 精确版本；安装器锁定 |
| Windows 开发机无法运行 MLX | 确定 | 开发/CI 受限 | e2e 沿用 mock 方案；真机测试项集中在 L1/L3 |
| Qwen3-VL-Thinking 变体输出思考文本污染 JSON | 低 | 解析失败 | 选 Instruct 变体（不用 Thinking）；JSON 截取逻辑 v1 已有 |

## 8. 发布计划（相对工期）

| 里程碑 | 内容 | 出口条件 | 工期 |
| --- | --- | --- | --- |
| **L1 真机选型**（先于一切开发） | 在 M4 Max 真机手动跑通 mlx-vlm 三档模型（+gemma-4-31b 观察组）：实测单张耗时、JSON 首过/修复后遵从率、内存峰值；用 v1 金标准集 200 张测分组 F1 | 产出《本地模型选型实测报告》，确定默认档位与性能预算定稿（回填 6.3 表） | 3–5 天 |
| **L2 引擎集成** | 模型管理器（下载/进度/删除/预检）+ mlx server 子进程管理 + 运行模式切换 + JSON 修复重试 + concurrency=1 调度 | KR-L1/L4/L6 达标；断网全流程跑通；下载中断续传实测 | 1.5–2 周 |
| **L3 打磨发布** | 安装器更新（MLX 依赖拆分）、设置页内存/磁盘提示、文档、真实拍摄集 UAT（重点：千张挂机一夜的断点续跑与稳定性） | KR-L2/L3/L5 达标；合入 main 发版 v2.0 | 1 周 |

单人 + AI 辅助，L1→L3 合计约 **3–4 周**（L1 依赖拿到 M4 Max 真机）。

## 9. Open Questions（评审拍板项）

| # | 问题 | 建议默认 |
| --- | --- | --- |
| 1 | 默认档位：30B-A3B（质量优先）还是 8B（兼容 36GB 机型） | 按物理内存自动推荐：≥48GB → 30B-A3B；36GB → 8B；用户可手动改 |
| 2 | API 模式是否在 v2 安装包中保留 | 保留（代码零成本，外勤赶时间场景有价值） |
| 3 | 多图拼批优化（单次推理 2–4 图）是否进 L2 | 不进，列 L3 可选（先保证串行正确性） |
| 4 | 模型下载源默认走 hf-mirror 还是 HF 官方 | 默认 hf-mirror（国内用户为主），设置页可切官方 |
| 5 | v2 是否同时作为 v1 的替代发版（本地版设为默认模式） | 是：v2.0 安装后默认本地模式，API 为高级选项 |
